# Development / test dependencies.
#
# The source of truth is pyproject.toml -> [project.optional-dependencies].dev
# (pyyaml, pydantic, pytest, pytest-asyncio, pytest-cov, hypothesis, ruff, pymodbus).
#
# Install the project in editable mode together with the dev extras:
-e .[dev]
